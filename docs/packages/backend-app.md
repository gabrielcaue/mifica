# Package: `com.mifica`

Este pacote contém o ponto de bootstrap da aplicação Spring Boot (`MificaApplication`).
Aqui a JVM sobe o contexto IoC, autoconfiguração e escaneamento de componentes.
É o entrypoint do monólito modular que orquestra os demais bounded contexts.
