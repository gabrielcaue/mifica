# Package: `mifica-flutter/lib/config`

Armazena configuração-base do app Flutter (ex.: cliente de API e parâmetros globais).
É o ponto de partida para conectar o mobile ao backend Java.
Serve como fundação para escalar o app nativo sem bagunçar o código.
