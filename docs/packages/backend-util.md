# Package: `com.mifica.util`

Agrupa utilitários de segurança JWT (geração, parsing, expiração e assinatura).
Fornece funções reutilizáveis para suporte criptográfico e autenticação.
Evita duplicação e mantém responsabilidades técnicas comuns em um ponto único.
