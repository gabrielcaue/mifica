# Package: `com.mifica.config`

Centraliza configuração transversal: `SecurityFilterChain`, CORS, Redis client beans e Web3 provider.
Define policy de segurança, serialização e wiring de infraestrutura via DI do Spring.
É a camada de configuration-as-code que garante comportamento consistente por ambiente.
