# Package: `com.mifica.repository`

Reúne repositórios Spring Data/JPA para acesso ao banco com interfaces declarativas.
Entrega operações CRUD, consultas derivadas e pontos para queries customizadas.
É a camada de data access que abstrai SQL e mantém o domínio desacoplado da persistência.
