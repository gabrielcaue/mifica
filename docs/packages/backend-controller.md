# Package: `com.mifica.controller`

Implementa a borda HTTP da API (REST controllers) com mapeamento de rotas e contratos JSON.
Concentra validação de entrada, status codes, autorização por endpoint e delegação para serviços.
É a camada de interface externa, mantendo separação clara entre transporte e regra de negócio.
