# Package: `mifica-frontend/src/hooks`

Contém hooks customizados (ex.: detecção de tamanho de tela para responsividade).
Isso deixa o comportamento reutilizável e o código das páginas mais limpo.
Na prática, transforma lógica repetida em função pronta para uso.
