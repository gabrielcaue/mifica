# Package: `com.mifica.dto`

Define DTOs de request/response para desacoplar o modelo de API das entidades JPA.
Reduz vazamento de detalhes internos, melhora versionamento e facilita validações granulares.
Na prática, é o anti-leak contract layer entre cliente e domínio persistido.
