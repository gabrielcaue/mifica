# Package: `mifica-frontend/src/services`

Centraliza comunicação com a API Java (requisições HTTP e configuração base).
Isso evita repetir URL/header em cada tela do frontend.
É a ponte entre interface web e backend.
