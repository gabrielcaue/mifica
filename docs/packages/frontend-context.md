# Package: `mifica-frontend/src/context`

Guarda estado compartilhado de autenticação para várias páginas ao mesmo tempo.
Assim, login/logout e dados do usuário ficam centralizados e consistentes.
É o ponto único para controlar sessão no frontend.
