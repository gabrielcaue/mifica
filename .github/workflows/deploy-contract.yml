name: Deploy Badge Contract (Deprecated)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use .github/workflows/deploy-badge-contract.yml instead."
